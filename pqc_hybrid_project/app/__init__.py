"""
Application package for the PQC hybrid demo.

This package encapsulates the FastAPI application and its
cryptographic helpers.  It is installed as a package inside the
Docker image so that ``uvicorn app.main:app`` resolves correctly.
"""